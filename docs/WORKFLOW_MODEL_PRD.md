# Workflow Model — PRD (v2)

## Goal

Define the v2 workflow model, including the replacement of `STATUS` with `WORKFLOW`, stage and node structure, dependencies, output artifacts, status semantics, recursive workflow composition, and managed workflow hydration rules.
